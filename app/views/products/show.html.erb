<%= @product.name %>
$<%= @product.purchase_price %>
<%= link_to "Delete|", product_path, method: :delete, data: {confirm: "Are you sure you want to delete this product?"} %>
<%= link_to "Edit|", edit_product_path %>
<%= link_to "Return", products_path %>
<br>
<% @product.descriptions.each do |d| %>
  <%= d.detail.name %>: <%= d.parameter %>
  <br>
<% end %>
<br>
<%= link_to "Add a new description to the product", new_product_description_path(@product) %>
<br>
<%= link_to "View all data types", details_path %>
<br>

<%= link_to "Create a product that's similar to <#{@product.name}> in one click !", new_similar_product_path %>
